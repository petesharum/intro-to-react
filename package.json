{
  "schema": "https://json.schemastore.org/package.json",
  "name": "intro-to-react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npm_config_yes=true npx serve .",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
